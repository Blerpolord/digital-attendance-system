// student.h
#ifndef STUDENT_H
#define STUDENT_H
#include <string>
using namespace std;

class Student {
public:
    string indexNumber;
    string name;
    string programme;
    Student(string idx, string n, string prog);
};
#endif

// student.cpp
#include "student.h"
Student::Student(string idx, string n, string prog) {
    indexNumber = idx;
    name = n;
    programme = prog;
}
